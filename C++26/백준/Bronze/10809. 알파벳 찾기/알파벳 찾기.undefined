#include <bits/stdc++.h>

using namespace std;

int main() {
	string a;
	vector<int> alpha(26, -1);
	
	cin >> a;
	for (int i = 0; i < a.length(); i++) {
		if (alpha[a[i] - 'a'] == -1) {
			alpha[a[i] - 'a'] = i;
		}
	}

	for (auto b : alpha) {
		cout << b << " ";
	}
}