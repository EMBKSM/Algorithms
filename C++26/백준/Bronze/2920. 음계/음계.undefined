#include <bits/stdc++.h>

using namespace std;

int main() {
	string front = "1 2 3 4 5 6 7 8";
	string back = "8 7 6 5 4 3 2 1";

	char temps[16];
	cin.getline(temps, 16);
	string temp(temps);
	cout << ((temp == front) ? "ascending" : (temp == back) ? "descending" : "mixed");
}