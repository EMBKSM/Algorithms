#include <bits/stdc++.h>

using namespace std;

int main(){
    string numbers;
    cin >> numbers;
    
    int answer= 0;
    
    for(int i = 0 ; i< 5;i++){
        answer += pow(numbers[i]-'0',5);
    }
    cout << answer;
}